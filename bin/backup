#!/bin/sh -e
#
# Script to run this backup
#

. $(dirname $0)/common

<<EOF cat
Backing up ${BACKUP_ROOT}

EOF

avoid_running_backup

cd "${BACKUP_ROOT}"

exec duplicacy \
    backup \
    -stats \
    -limit-rate 20000 \
    "$@"
